# TestOps 产品设计规范

> 本规范用于统一 `TestOps` 的产品设计、交互表达、页面结构与文案口径，降低跨页面体验差异，提升 QA 工程师、测试负责人、交付团队在「建项目 → 上传资料 / 导入用例 → 编辑 → 评审 → 批准 → 发布」链路中的操作效率与信任感。
>
> 当前适用范围：`TestOps` Web 管理端。
>
> **规范关系**：本规范在 [B端产品设计规范](/D:/Menusifu/AIQA/B端产品设计规范.md) 的通用 B 端规则基础上，结合 TestOps 测试工作台场景做了领域适配。借鉴映射详见 [附录 D](#附录-db-端规范借鉴映射)。适配设计说明见 [docs/superpowers/specs/2026-07-05-b-end-design-adaptation-design.md](/D:/Menusifu/TestOps/docs/superpowers/specs/2026-07-05-b-end-design-adaptation-design.md)。

## 一、产品定位

### 1.1 产品目标

`TestOps` 是测试用例工作台，用于管理项目、资料、测试用例草稿、评审记录与发布结果，并为下游自动化脚本生成流程提供结构完整、状态可信的测试数据。

### 1.2 核心用户

1. QA 工程师：上传资料、生成或导入用例、编辑步骤与预期、维护测试数据。
2. 测试负责人：评审用例质量、推进批准与发布、追踪任务状态。
3. 交付团队：查看项目状态、复用已发布用例、衔接后续自动化流程。

### 1.3 产品气质

1. 专业：信息表达准确，状态定义清晰，不制造歧义。
2. 清晰：页面任务单一，主次分明，避免过度装饰。
3. 可靠：操作有反馈、状态可追踪、关键动作可确认。

## 二、产品设计原则

### 2.1 一致性

1. 同一对象在全站使用统一名称。
2. 同一状态在全站使用统一颜色、文案与交互。
3. 同一组件在不同页面保持相同结构与行为。

### 2.2 反馈性

1. 用户发起操作后，界面需立即反馈。
2. 长耗时任务必须展示处理中状态，不可静默等待。
3. 成功、失败、校验错误、空状态需明确区分。

### 2.3 效率优先

1. 页面只突出一个主任务。
2. 高频操作应在首屏可达。
3. 优先让用户“识别并执行”，而非“记忆后操作”。

### 2.4 可控性

1. 删除、发布、归档等关键动作必须二次确认。
2. 用户应能清楚知道当前对象所处状态，以及下一步可执行动作。
3. 不允许系统在用户无感知情况下自动改变核心业务状态。

### 2.5 状态完整

每个交互组件至少应具备以下状态：

1. 默认
2. hover
3. focus
4. active
5. disabled
6. loading
7. error（如适用）

## 三、信息架构与核心对象

### 3.1 核心模块

1. 项目
2. 文档资料
3. 测试用例
4. 评审与发布
5. 生成任务
6. 自动化计划
7. 设置

### 3.2 核心对象定义

1. 项目：用例生成、评审、发布的工作容器。
2. 文档：PRD、Swagger、Figma 等输入资料。
3. 测试用例：可编辑、可评审、可发布的结构化测试内容。
4. 评审记录：围绕用例产生的评论、退回、批准、驳回、发布动作记录。
5. 生成任务：由资料生成测试用例的任务实例。

### 3.3 核心流程

1. 新建项目
2. 上传资料或导入用例
3. 生成 / 写入用例草稿
4. 编辑与补充
5. 评审
6. 批准
7. 发布

## 四、页面设计规范

### 4.1 项目列表页

适用于首页、项目总览页。

1. 页面目标：快速查看项目状态并进入工作区。
2. 主操作：`新建项目`。
3. 默认展示信息：
   - 项目名称
   - 项目代号
   - 项目描述摘要
   - 文档数
   - 草稿数
   - 已发布数
   - 项目状态
4. 列表视图要求：
   - 支持进行中 / 已归档切换
   - 项目名称过长时单行截断
   - 空状态需说明下一步动作
5. 归档项目默认不出现在主列表，可通过筛选查看。

### 4.2 项目工作区页

适用于项目首页、工作台总览页。

1. 页面目标：承接项目内下一步动作。
2. 页面内容应围绕当前项目展开，不混入跨项目信息。
3. 推荐结构：
   - 项目标题区
   - **主流程步骤条**（见 4.2.1）
   - **待办摘要卡片**（待评审数、失败任务数等，优先于装饰性文案）
   - 当前阶段或下一步提示
   - 文档入口
   - 用例入口
   - 评审入口
   - 生成任务入口
4. 若项目已有待处理草稿，应优先提示“进入预览评审”。

#### 4.2.1 控制台信息优先级（借鉴 B 端 3.1 控制台页）

1. **第一优先级**：阻断主流程的异常与待办（失败任务、待评审草稿、已归档锁定）。
2. **第二优先级**：核心统计（文档数、草稿数、已发布数、各状态用例数）。
3. **第三优先级**：操作入口与下一步引导。
4. 装饰性说明、品牌表达不得抢占待办与统计的视觉焦点。

#### 4.2.2 主流程步骤条

项目工作区应呈现线性主链路进度：

`新建项目 → 上传资料 → 生成用例 → 编辑补充 → 评审 → 发布`

1. 步骤条仅用于**线性流程**，禁止用于非线性分支。
2. 须展示当前步骤、已完成步骤与未完成步骤的视觉差异。
3. 用户须能感知「当前缺什么才能进入下一步」（如无文档则高亮上传资料）。
4. 步骤切换由用户主动操作触发，系统不可静默跳过步骤。

### 4.3 文档资料页

适用于上传 PRD、Swagger、Figma 等资料的页面。

1. 页面目标：完成资料录入并触发用例生成。
2. 支持两类输入方式：
   - 本地上传
   - 链接地址
3. 每条文档至少展示：
   - 文档名称
   - 文档类型
   - 来源方式
   - 来源地址或文件标识
4. 文档列表支持多选，以便批量生成。
5. 当无文档时，空状态需明确提示支持的资料类型与下一步。
6. `生成测试用例` 为主按钮，必须依赖已选中文档。

### 4.4 测试用例列表页

适用于项目下的用例总览页。

1. 页面目标：筛选、浏览、进入编辑或评审。
2. 推荐布局：左侧目录树，右侧列表与筛选工具栏。
3. 检索条件建议：
   - 状态
   - 关键词搜索（标题 / 模块 / 功能点）
   - 目录范围
   - 优先级（当页面已提供该维度时）
   - 用例类型（当页面已提供该维度时）
4. 列表默认字段：
   - 用例标题
   - 模块
   - 功能点
   - 优先级
   - 状态
   - 步骤数
   - 更新时间
5. 高优先级与主状态信息可强化，但不可喧宾夺主。
6. 空结果页分两类处理：
   - 没有任何用例
   - 有用例但当前筛选无结果
7. 查询条件较多时，应提供 `高级查询` 展开 / 收起入口。
8. 查询结果区应展示当前结果摘要，如 `显示 12 / 48 条`。
9. 当使用高级查询后，结果区顶部应展示当前查询轨迹，便于用户理解当前筛选上下文。

### 4.5 用例详情 / 编辑页

适用于单条用例查看与编辑。

1. 页面目标：编辑结构化测试内容并完成保存。
2. 推荐布局：主编辑区 + 侧边元数据区。
3. 主编辑区建议包含：
   - 标题
   - 模块
   - 功能点
   - 优先级
   - 前置条件
   - 操作步骤
   - 预期结果
   - 测试数据
   - 标签
   - 清理 / 收尾
4. 自动化结构场景可增加：
   - 运行环境
   - 浏览器
   - 基础 URL
   - 入口路径
   - 定位提示
   - 步骤断言
5. 步骤表格要求：
   - 行号固定可见
   - 支持逐行编辑
   - 字段错误在单元格内直接提示
6. 保存按钮文案统一为 `保存修改`。

### 4.6 评审页

适用于项目级评审或单用例评审。

1. 页面目标：查看内容、给出意见、完成批准 / 退回 / 驳回 / 发布。
2. 页面必须清楚区分：
   - 当前内容
   - 当前状态
   - 评审历史
   - 可执行动作
3. 操作按钮建议顺序：
   - 次操作：退回修改、驳回
   - 主操作：批准
   - 满足条件后展示：发布
4. 评审意见为可选输入，但关键负向操作建议填写原因。
5. 评审历史需按时间倒序展示。

### 4.7 生成任务页

适用于查看 AI 生成或处理任务状态。

1. 页面目标：让用户判断任务是否成功、失败原因是什么、是否需要重试。
2. 列表建议展示：
   - 任务名称或批次
   - 关联项目
   - 任务状态
   - 创建时间
   - 完成时间
   - 失败原因摘要
3. 失败任务的错误信息应可展开查看。
4. 不使用只有颜色没有文字的状态表达。

### 4.8 设置页

适用于环境配置、默认项说明、系统信息查看。

1. 页面目标：查看并维护全局配置。
2. 配置项应按主题分组，不建议长表单无分段堆叠。
3. 只读信息与可编辑信息应视觉区分。

## 五、通用交互规范

### 5.1 页面跳转载体

1. 新页面：
   - 适用于完整任务流程，如新建用例、查看用例详情。
2. 弹窗：
   - 适用于轻量确认，如归档、恢复、删除、发布确认。
3. 抽屉：
   - 适用于需要参考原页面信息的中等复杂操作。

### 5.2 主次操作

1. 每页仅保留一个主按钮。
2. 主按钮使用主题强调色。
3. 次要动作使用次按钮或幽灵按钮，不可与主操作抢焦点。

### 5.3 危险操作

以下操作必须使用危险语义，并二次确认：

1. 删除
2. 驳回
3. 发布后锁定
4. 项目归档（若会影响默认可见性）

### 5.4 加载反馈

1. 列表加载优先使用骨架屏或局部加载态。
2. 按钮提交中显示 `进行中` 文案或 loading 状态。
3. 不允许用户在处理中重复触发同一提交动作。

### 5.5 查询操作规范

1. 所有检索条件必须提供明确标题，不可仅依赖占位提示表达条件含义。
2. 查询条件顺序默认按“查询频率 + 业务语义”排序，而不是按接口字段顺序平铺。
3. 默认每行展示 3 个检索条件；每行承载 2–4 个为可接受范围。
4. 检索条件标题与输入框同一行时，标题统一右对齐，便于扫视关联。
5. 默认展开的检索条件总数不超过 8 个。
6. 当检索条件超过 2 行承载范围时，超出部分默认收起，通过 `高级查询` 或 `+ N 个更多条件` 展开。
7. **查询条件顺序须与列表默认列顺序保持一致**，避免筛选与结果列认知脱节。
8. **语义关联的条件须相邻排列**（如目录、模块、功能点）。
9. 执行查询后，结果列表自动回到第一页，避免用户停留在旧分页位置。
10. 执行查询后，保留当前全部筛选条件；从详情页返回列表时同样保留（见 5.12）。
11. 当检索条件达到 5 个及以上时，必须同时提供 `重置` 按钮。
12. `重置` 后应恢复页面初始查询状态，包括：查询条件、排序方式、分页位置、列显隐（如页面支持）。
13. `重置` 须防重复点击（如 1 秒内仅允许 1 次）。
14. 当使用高级查询条件后，结果区顶部应展示查询轨迹。
15. 查询轨迹需支持逐项删除单个条件。
16. 删除某个查询条件后，系统需基于剩余条件立即刷新结果，并保留用户当前排序方式。
17. 无结果时，需显示明确提示（如「未找到相关记录，请调整查询条件后重试」），不可让用户误判为页面加载失败。
18. 查询字段优先使用用户可理解的业务字段，不优先暴露内部 ID 作为主要筛选入口。
19. 关键词查询默认支持模糊匹配；精确匹配须通过独立开关或控件区分。

**筛选控件细则**（借鉴 B 端 3.5.2）：

1. 日期类：支持范围选择；任务/评审类列表默认「最近 30 天」为常用快捷范围（若页面提供日期筛选）。
2. 文本类：提供「精确匹配」开关（若业务需要）。
3. 多选枚举：选中 ≤3 项时展示具体选项；超过 3 项时展示「已选 X 项」。
4. 折叠入口文案统一为 `+ N 个更多条件` 或 `高级查询`。

### 5.6 编辑操作规范

1. 单条编辑保存成功后，应返回原记录所在上下文，并刷新为最新数据。
2. 在筛选列表中编辑记录后，若该记录已不满足原筛选条件，则返回列表后不再展示该记录。
3. 编辑提交失败时，必须保留用户已填写内容，不可清空表单或回退为旧值。
4. 编辑页存在必填项缺失时，提交后需自动定位到第一个错误字段。
5. 对主要标识字段需至少校验以下三类问题：
   - 空值
   - 重复值（当业务要求唯一时）
   - 首尾空格或无效空格

### 5.7 删除操作规范

1. 删除属于高风险操作，必须二次确认。
2. 删除确认弹窗标题应直接说明对象与动作，如 `确认删除用例？`。
3. 删除确认文案需明确说明影响范围；若删除后不可恢复，必须直接写明。
4. 删除单条记录时，文案应带对象名称；批量删除时，文案应带对象数量。
5. 删除成功后，被删除记录需立即从当前列表中隐藏。
6. 删除成功后，列表应停留在原分页与原筛选上下文，而不是跳回默认页。
7. 若对象存在业务关联且当前不允许删除，前端必须明确提示不可删除原因。
8. 若对象存在业务关联但允许删除，确认文案需明确说明删除后的联动影响。
9. 删除失败时，原列表数据与选中状态应保持可理解，不可出现“视觉上已删除、实际未删除”的假成功状态。
10. 批量删除时，需明确成功、失败、部分成功三种结果反馈。

### 5.8 新增操作规范

1. 列表页新增记录排在首页首行，便于用户立即看到。
2. 表单内新增明细行（如步骤）排在末尾，符合录入顺序。
3. 提交失败保留已输入内容；主要标识字段校验空值、重复、首尾空格。

### 5.9 保存与翻页规范

1. 长耗时保存展示进度或 loading，禁止重复点击。
2. 保存成功后刷新当前上下文数据；编辑页保存不无故跳转。
3. 带筛选条件的翻页须同步执行查询，各页数据均符合当前条件。
4. 全选仅选中当前页；取消单条勾选时全选状态联动取消。
5. 跨页勾选状态在翻页后保留（批量能力就绪后）。

### 5.10 导出规范

1. **命名**：`{页面标题}-{YYYYMMDD}{流水号}`，如 `测试用例-20250705000001`。
2. **字段**：导出字段与页面当前展示列一致，不擅自增减。
3. **格式**：导出数据格式（日期、状态文案）与页面展示一致。
4. **范围**：支持「导出当前页」「导出全部筛选结果」；批量导出需先勾选（能力就绪后）。
5. **JSON 导入导出**：作为 TestOps 主交换格式，需保持 schema 稳定与版本可追溯。

### 5.11 取消、返回与关闭规范（借鉴 B 端 3.6）

| 动作 | 含义 | 行为 |
|---|---|---|
| **取消** | 终止当前编辑，不跳转 | 关闭弹窗或停留当前页；有未保存修改时须二次确认 |
| **返回** | 层级回退 | 回到触发跳转的原页面（含原筛选、分页、目录选中态） |
| **×** | 关闭浮层 | 弹窗右上角关闭；全站统一是否同时提供「取消」按钮 |

1. 「取消」与「返回」不可混用同一按钮承担两种语义。
2. 取消确认文案示例：`确认取消？已修改内容将不保存。`
3. 弹窗规则：危险操作使用居中 Modal；行内轻量确认可使用 Popconfirm（见 10.6）。

### 5.12 跨页上下文记忆

从列表进入详情 / 编辑 / 评审后返回时，须保留：

1. 筛选条件与查询轨迹
2. 排序规则
3. 页码与每页条数
4. 目录树选中节点
5. 跨页勾选状态（批量能力就绪后）

### 5.13 导入规范

1. 导入超过 2 秒须展示进度或处理中状态。
2. 完成后须给出摘要：`成功 X 条，失败 Y 条`；失败须可查看原因或定位失败行。
3. 部分成功时须区分「全部成功 / 部分成功 / 全部失败」三种反馈。
4. 导入失败不得清空用户已选文件或已填配置。
5. JSON 导入须校验 schema 与必填字段，错误贴近字段或文件级说明。

## 六、字段与数据规范

### 6.1 字段分类

`TestOps` 常见字段分为以下几类：

1. 标识类：项目 ID、项目代号、用例 ID、目录 ID、任务 ID
2. 名称类：项目名称、文档名称、用例标题、模块、功能点、标签
3. 枚举类：状态、优先级、文档类型、来源方式、自动化框架、浏览器
4. 长文本类：项目描述、评审意见、前置条件、操作步骤、预期结果、错误信息
5. 时间类：创建时间、更新时间、发布时间、完成时间
6. 数量类：文档数、草稿数、步骤数、任务数

### 6.2 名称类字段规范

1. 支持中英文、数字、常用符号。
2. 禁止首尾空格。
3. 允许中间空格。
4. 不支持 emoji。
5. 长文本展示时应保留原文，列表中可截断。

### 6.3 标识类字段规范

1. ID 由系统生成，不暴露为主要编辑字段，不作列表主筛选入口。
2. 项目代号可人工输入，但需校验唯一性或在业务层明确冲突处理方式。
3. 用户可见的编码字段应保证稳定，不随普通编辑动作频繁变化。

**标识语义区分**（借鉴 B 端 2.2）：

| 类型 | 示例 | 用途 | 展示规则 |
|---|---|---|---|
| ID | 用例 ID、任务 ID、文档 ID | 系统内部关联 | 详情页或调试区可见；列表默认隐藏 |
| 编码 | 项目代号 | 人工可维护的业务标识 | 列表可展示；支持排序 |
| 批次号 | 生成任务批次、导出流水号 | 追溯与对账 | 任务列表展示；支持排序 |

4. 界面不得要求用户手工输入或记忆内部 ID（如 `documentId`）完成业务操作。

### 6.4 枚举类字段规范

1. 应通过下拉、单选、分段控件等结构化方式输入。
2. 相同枚举值在全站统一文案。
3. 状态不可同时使用多个近义词。

### 6.5 时间类字段规范

1. 展示格式全站统一。
2. 列表中默认按时间倒序展示最新内容。
3. 若时间与业务决策强相关，应展示到时分秒。
4. 区分「日期型」与「日期 + 时间型」：创建日期仅展示年月日；操作时间、更新时间需精确到时分秒。
5. 日期类字段天然支持排序，无需合计。

### 6.6 数值类字段规范

适用于步骤数、文档数、草稿数、任务数等量化数据。

1. 列表中数量类字段支持排序；是否展示合计取决于页面是否提供统计摘要。
2. 数量展示不使用千分符；若后续引入金额类字段，金额超过千位时使用美式千分符。
3. 精度以业务场景为准；TestOps 当前以整数步数、计数为主，不引入 ERP 级小数精度配置。

### 6.7 字段长度上限

| 类型 | 默认上限 | 适用字段 |
|---|---|---|
| 基础信息 | 100 字符 | 项目名称、用例标题、模块、功能点、Skill 名称、标签 |
| 详细描述 | 256 字符 | 项目描述、评审意见摘要、长备注 |
| 标识编码 | 20 字符 | 项目代号、外部引用编码 |
| 长文本 | 不限（表单内） | 前置条件、操作步骤、预期结果、测试数据 |

超出上限时：输入框失去焦点后飘红提示「内容超出限制，请重新输入」，并阻止提交。

### 6.8 目录层级规范（借鉴 B 端 4.3 方法论）

适用于测试用例目录树：

1. 用例仅允许挂在**叶子目录**节点；非叶子节点用于分类，不直接承载用例（若产品允许例外，须在页面说明中明示）。
2. 删除目录前须校验：存在子目录或下属用例时不可删除，并给出明确原因。
3. 删除确认文案示例：`目录下存在子目录或用例，不可删除。`
4. 目录名称在同一父级下不可重复。

## 七、状态流转规范

### 7.1 用例状态

当前建议标准状态：

1. `draft`：草稿
2. `needs_update`：待修改
3. `approved`：已批准
4. `published`：已发布
5. `rejected`：已驳回

### 7.2 状态说明

1. 草稿：已生成或已创建，但尚未通过评审。
2. 待修改：评审认为需补充或修正后再提交。
3. 已批准：内容通过评审，可进入发布动作。
4. 已发布：内容已对下游可用，默认锁定核心内容。
5. 已驳回：当前版本被否决，不进入发布。

### 7.3 状态流转规则

1. 草稿可进入：待修改、已批准、已驳回
2. 待修改可回到：已批准或已驳回
3. 已批准可进入：已发布
4. 已发布默认不可编辑核心内容，如需修改应明确是否新建版本或退回草稿

## 八、表格与筛选规范

### 8.1 筛选条件顺序

默认按照“最常用、最能缩小范围、最符合业务心智”的顺序排列。

推荐顺序：

1. 状态
2. 关键词
3. 目录 / 分类
4. 其他扩展条件

补充规则：

1. 查询标题需使用用户易理解的字段名称，如 `功能点`、`优先级`、`目录`，不直接展示内部技术字段名。
2. 同类条件的交互方式需统一，例如枚举类统一使用下拉或分段控件，避免同一类条件一处下拉、一处自由输入。
3. 若页面同时存在基础查询与高级查询，基础查询区必须保留最高频的 3-5 个条件。

### 8.2 表格字段顺序

默认遵循：

1. 核心识别信息
2. 业务属性
3. 状态信息
4. 时间信息
5. 操作

补充规则：

1. 默认按更新时间或创建时间倒序展示最新记录，除非页面另有明确业务排序。
2. 当列表信息较多时，必须定义默认展示字段与收起字段，不允许无差别平铺全部列。
3. 若页面支持统计信息，应在表格上方展示结果汇总信息，如总条数、各状态数量，而不是要求用户滚动统计。

### 8.3 操作数量控制

1. 行内默认展示 2-3 个高频操作。
2. 超出部分收纳进更多菜单。
3. 不在表格中平铺低频危险操作。
4. 行内危险操作不与主操作并列抢占视觉焦点。
5. 批量操作仅在用户勾选记录后出现或可用。
6. 批量操作区应至少支持清空选择，避免用户误操作残留。

### 8.4 查询结果反馈

1. 查询成功后，结果区需同步刷新以下内容：
   - 列表数据
   - 总条数或结果摘要
   - 空状态或无结果提示
2. 查询无结果时，提示文案需优先引导用户调整条件，不使用系统异常类措辞。
3. 若结果区存在统计摘要，统计值必须与当前筛选结果保持一致。

### 8.5 字段对齐

1. 表头对齐方式必须与对应列数据一致。
2. 数值类（步骤数、文档数、草稿数、任务数）：右对齐。
3. 固定长度类（日期、日期时间、ID、项目代号、状态）：左对齐。
4. 可变长度类（名称、标题、操作列）：左对齐。

### 8.6 表格交互细则

1. **空值展示**：无信息单元格留空，不使用 `-`、`/` 占位，避免导出脏数据。
2. **内容截断**：超长文本列表中省略显示，悬浮展示完整内容。
3. **行高亮**：悬浮行与选中行使用不同背景色区分。
4. **滚动条**：表格横向滚动条固定于表格底部，纵向滚动条固定于右侧，采用显性显示。
5. **排序**：可排序字段点击表头触发；支持全局排序（跨页一致），翻页后保留排序规则。
6. **列管理**：支持默认展示列与可选列；用户自定义列显隐后，随账号保存（能力就绪后落地）。
7. **分页**：
   - 数据不足一屏（如 ≤10 条）可不显示分页。
   - 默认提供 10 / 20 / 50 / 100 条每页选项；用户选择后随账号保存。
   - 翻页后保留筛选条件、排序与跨页勾选状态（批量能力就绪后）。
8. **合计与摘要**：筛选结果的全局统计（总条数、各状态数量）展示在表头下方或结果摘要区，不要求用户滚至表格底部查看。

### 8.7 列表类型与默认字段顺序

**主数据型列表**（项目列表、Skill 列表）：

1. 名称 / 标题
2. 代号 / 类型
3. 状态
4. 统计信息（文档数、草稿数等）
5. 更新时间
6. 操作

**任务 / 单据型列表**（生成任务、评审队列）：

1. 创建时间 / 业务时间
2. 任务标识 / 用例标题
3. 状态
4. 关联对象
5. 操作

**明细型表格**（用例步骤、测试数据）：

1. 序号
2. 步骤 / 键名
3. 内容 / 值
4. 操作

## 九、表单规范

### 9.1 基础规则

1. 必填字段必须明确标识。
2. 标签文案优先使用业务名称，不用技术术语代替。
3. 输入控件类型应与字段类型匹配。

### 9.2 占位提示

占位提示仅用于补充输入格式或举例，**不替代字段标签**。

占位提示分三类（借鉴 B 端 3.3.2）：

1. **格式提示**：告知输入规范，如 `YYYY-MM-DD`、`JSON 文件`。
2. **业务限制**（≤8 字）：如 `最多 100 字`、`必填`。
3. **通用引导**：如 `请输入`、`请选择`。

示例：

1. `请输入项目名称`
2. `请输入名称、模块或功能点`
3. `请填写评审意见（选填）`

### 9.3 错误定位

1. 提交失败时，自动定位到首个校验错误字段。
2. 错误信息贴近字段显示。
3. 不使用“校验失败”这类无定位能力的笼统提示替代字段提示。

### 9.4 输入焦点行为（借鉴 B 端 3.16 通用部分）

1. **新建空白**：激活后占位消失，光标位于首字符位。
2. **编辑已有内容**：激活时全选已有文本，便于快速替换。
3. **下拉框**：激活时展开选项；有默认值时保留默认选中。
4. **实时校验**：失去焦点或输入中按规则校验，错误贴近字段展示，修正后自动消失。

### 9.5 表单 + 表格混合页（用例编辑、步骤表）

1. 简单表单区字段建议 ≤10 个可见主字段；更多字段分段或侧边元数据区承载。
2. 步骤表列数较多（>6）时，可考虑斑马纹辅助阅读。
3. 关键识别信息（用例标题、状态）在长页滚动时**固定顶栏**，避免丢失上下文。
4. 步骤表底部可展示本页统计（如「共 N 步」）。

## 十、提示与消息规范

### 10.1 提示类型

1. 字段级错误：飘红提示
2. 关键确认：弹窗
3. 成功 / 轻提示：Toast
4. 页面级异常：错误页或告警区块

适用规则：

1. 飘红提示：用于必填遗漏、格式错误、长度超限、字段级业务校验失败。
2. 弹窗：用于删除、发布、归档、恢复等关键确认，以及会影响当前任务继续进行的重要异常。
3. Toast：用于保存成功、导入完成、状态已更新、所选内容无变化等轻量反馈。
4. 错误页或告警区块：用于接口不可用、页面加载失败、核心数据获取失败等页面级问题。

### 10.2 提示文案原则

1. 直接说明发生了什么。
2. 尽量说明用户下一步怎么做。
3. 避免纯技术报错直接暴露给普通用户。

示例：

1. 好：`请选择要上传的文件。`
2. 好：`无法解析用例文件，请确认 JSON 格式是否正确。`
3. 差：`请求失败`
4. 差：`Error code 500`

补充规则：

1. 成功提示默认不打断用户操作，短暂停留后自动消失。
2. 失败提示默认不清空用户已输入内容。
3. 同一操作触发多个字段错误时，优先定位首个错误字段，其余错误保留在对应字段下展示。
4. 阻断式提示必须提供明确操作出口，如 `取消`、`确认删除`、`返回列表`。

### 10.3 空状态规范

空状态必须回答两个问题：

1. 为什么这里没有内容
2. 用户现在可以做什么

### 10.4 提交后的错误定位

1. 新增、编辑、导入配置等提交型操作，若存在字段错误，提交后需自动聚焦到第一个错误字段。
2. 第一个错误字段需同时满足以下条件：
   - 滚动进入可视区域
   - 获得焦点
   - 展示对应错误文案
3. 若错误位于折叠区域、Tab、抽屉分组或步骤表格中，系统需自动展开对应区域再定位。
4. 字段级错误优先使用贴近字段的飘红提示，不用 Toast 替代字段定位。

### 10.5 页面跳转后的反馈

1. 新页面承接完整流程时，必须保留返回入口，如面包屑或返回列表按钮。
2. 弹窗完成操作后，默认关闭并刷新原页面对应数据区域。
3. 抽屉完成操作后，默认保留原页面上下文，仅刷新受影响区域。

### 10.6 反馈组件扩展（借鉴 B 端 3.11）

| 组件 | 适用场景 |
|---|---|
| **Popconfirm** | 行内删除、Skill 回滚等轻量二次确认 |
| **Alert 静态条** | 项目已归档、Skill 未发布、未批准不可发布等持续态提醒 |
| **Badge** | 导航待办数（待评审、失败任务） |
| **Tooltip** | 图标按钮、截断文本的完整内容 |
| **录入校验** | 字段旁实时飘红，不用 Toast 替代 |

### 10.7 批量选择动态文案

1. 未选择：`已选择 0 项`
2. 单选：`已选择 1 条用例`（对象名随页面变化）
3. 多选：`已选择 X 条用例`（X 实时同步）

## 十一、文案与命名规范

### 11.1 命名统一

以下词汇建议全站统一：

1. 项目
2. 文档
3. 测试用例
4. 评审
5. 批准
6. 退回修改
7. 驳回
8. 发布
9. 归档
10. 恢复

### 11.2 禁止混用

以下近义词不建议混用：

1. `审核` 与 `评审`
2. `通过` 与 `批准`
3. `上线` 与 `发布`
4. `删除` 与 `移除`

### 11.3 按钮文案

按钮文案优先使用“动作 + 对象”或稳定短动词：

1. `新建项目`
2. `上传资料`
3. `导入用例`
4. `保存修改`
5. `发布用例`
6. `确认归档`

补充规则（借鉴 B 端 4.5 按钮命名）：

1. 列表页创建统一为 `+ 新增` 或 `新建{对象}`，不使用 `+ 新增 XXX 商品` 式冗长命名。
2. 表单提交统一 `保存` / `保存修改`，不用 `提交` 与 `保存` 混用同一语义。
3. 弹窗确认统一 `确定` / `取消`；危险确认使用 `确认删除` / `确认归档`。
4. 从列表选择关联对象时统一 `选择`，不用 `添加 XXX 分组`。
5. 行内操作默认 `编辑`、`删除`；`修改` 与 `编辑` 不混用。

### 11.4 文案格式与标点

1. 站在用户视角：说「你可查询」而非「系统支持查询」。
2. 同一概念全站一词一义；操作名与目标页标题匹配。
3. 报错说明具体问题与修正方式，如「用例标题不能为空」而非「校验失败」。
4. 语气友好：用「请填写」，不用「必须填写」；不责怪用户。
5. 统计数据用阿拉伯数字（如 `5 条`、`30%`）。
6. 以下场景**不使用句号**：输入框提示、表格单元格、按钮文案、标题、文字链。
7. 省略号统一半角 `…`；连接号统一半角 `-`。
8. 感叹号仅用于紧急提示，日常引导避免使用。

## 十二、视觉与组件规范

### 12.1 视觉方向

1. 浅色产品型界面
2. 以中性色为主，蓝色仅用于主操作、当前导航、focus 与状态强调
3. 视觉服务任务，不追求营销化表达

### 12.2 颜色角色

1. 页面背景
2. 面板背景
3. 次级背景
4. 边框
5. 正文
6. 辅助文字
7. 主强调色
8. 成功色
9. 危险色

### 12.3 组件统一

应统一以下组件：

1. 主按钮 / 次按钮 / 幽灵按钮 / 危险按钮
2. 输入框 / 文本域 / 下拉框
3. Tab
4. 状态徽章
5. 表格
6. 面包屑
7. 模态框
8. 空状态
9. 错误页

### 12.4 字体层级

| 场景 | 字号 | 字重 | 颜色 |
|---|---|---|---|
| 页面 / 模块标题 | 16px（1rem 级） | 粗体 | 正文色 |
| 导航一级 | 14–16px | 粗体 | 默认；选中为主题色 |
| 导航二级 | 14px | 常规 | 默认 |
| 表头 | 12–14px | 粗体 | 辅助色 |
| 表格数据 | 14px | 常规 | 正文色 |
| 页面说明 | 12px | 常规 | 浅灰辅助色 |
| 操作链接 | 12–14px | 常规 | 主题色 |
| 弹窗标题 | 16px | 粗体 | 正文色 |
| 字段说明（问号） | 12px | 常规 | 浅灰 |

### 12.5 导航与分页

1. **侧栏导航**：文案不超过 6 个汉字；顺序符合业务流程；当前页高亮（主题色 + 字重）。
2. **面包屑**：用于新页面承接完整流程时保留返回上下文。
3. **Tabs**：TestOps 以侧栏 + 面包屑为主，谨慎引入多页签；若使用，当前页签高对比，备选页签中性灰。
4. **步骤条**：仅用于线性多步向导（如导入向导）；禁止用于非线性分支流程。
5. **分页器**：列表 / 表格默认分页；详情 / 编辑页不分页；支持自定义每页条数。
6. **滚动条**：横向滚动条固定底部、纵向固定右侧；全局滚动条样式统一（宽度约 8px，圆角，中性色）。

### 12.6 图标

1. 图标采用语义化矢量图形，含义与功能强关联。
2. PC 端主业务流程操作**不建议**纯图标按钮；核心操作需配文字。
3. 通用图标（关闭、删除、提示）可使用；业务专有操作优先文字。
4. 新增特殊图标需与全站图标库风格一致。

### 12.7 快捷键

| 场景 | 按键 | 行为 |
|---|---|---|
| 登录 / 弹窗 | Enter | 触发确定 / 保存 |
| 弹窗 | Esc | 关闭 |
| 表单 | Tab | 跳转下一可编辑控件 |
| 删除确认弹窗 | Enter | 等同确定（与 B 端现状一致，需谨慎） |

### 12.8 反馈组件选型

| 场景 | 组件 |
|---|---|
| 字段错误 | 字段旁飘红 |
| 删除 / 发布 / 归档 | 居中确认弹窗 |
| 保存成功 / 轻量提示 | 顶部 Toast，自动消失 |
| 页面加载失败 | 错误区块或错误页 |
| 操作 >2 秒 | Loading / 进度提示 |
| 图标无文字说明 | Tooltip |

### 12.9 视觉交互状态（借鉴 B 端 4.9）

**按钮四态**：

1. 默认：主题色，无多余阴影
2. Hover：加深色或轻微阴影
3. Active：略缩或更深色
4. Disabled：灰色，不可点击，文案降对比

**输入框**：默认边框 → Focus 主题色边框 + 外发光 → Error 红色边框 + 字段旁说明。

**页面切换**：加载中显示 spinner 或骨架；过渡建议 200–300ms 淡入淡出；先加载新内容再隐藏旧内容。

**响应式**：优先支持 1366×768 至 1920×1080；小屏侧栏可折叠，核心按钮不可挤出首屏。

## 十三、可访问性与响应式规范

### 13.1 可访问性

1. 正文与背景对比度满足 WCAG AA。
2. 键盘焦点必须清晰可见。
3. 不能只依赖颜色表达状态，需配合文字。
4. 表单错误需可感知、可定位。

### 13.2 响应式

1. 小屏优先保证任务可完成，而不是保证桌面布局原样缩放。
2. 小屏下允许侧栏折叠、表格横向滚动、工具栏换行。
3. 不允许核心按钮被挤出首屏可操作区域。

## 十四、性能与质量基线

### 14.1 页面体验

1. 常用页面应在可接受时间内完成首屏展示。
2. 长列表需避免明显卡顿。
3. 大批量导入、生成、发布任务必须有进度或状态反馈。

### 14.2 重点压测场景

1. 导入 100 / 1000 / 10000 条用例的耗时与稳定性
2. 打开包含 100 / 1000 / 5000 条用例的项目列表表现
3. 单条用例包含大量步骤时的编辑性能
4. 批量生成与发布任务的状态同步时延

## 十五、TestOps 专有规范

以下内容为 TestOps 测试工作台特有要求，B 端规范未覆盖，须单独遵循。

### 15.1 追溯链展示

用例详情须能回答「内容从哪来、用什么规则生成」：

1. 关联文档名称与版本 / 来源
2. 生成任务 ID 或批次（可跳转）
3. 使用的 Skill 名称与发布版本
4. 生成时间与操作人（若可得）

追溯信息放在详情页侧边或折叠区，不可仅在调试接口中可见。

### 15.2 评审与发布双阶段

1. **批准**与**发布**为两个独立动作；未批准不可发布（前端 + 接口一致）。
2. 页面须同时展示：当前状态、可执行动作、不可执行原因（如「请先批准用例，再执行发布。」）。
3. 负向动作（退回修改、驳回）须记录评审意见或默认说明。

### 15.3 AI 生成态

| 状态 | 界面要求 |
|---|---|
| 排队 / 生成中 | 明确 processing，禁止重复提交 |
| 成功 | Toast + 刷新列表或跳转预览 |
| 失败 | 可展开错误原因；提供重试或修正入口 |
| 部分成功 | 摘要说明成功/失败条数 |

### 15.4 锁定态与只读态

| 场景 | 规则 |
|---|---|
| 已发布用例 | 核心字段只读或编辑后不可直接发布，策略须全站一致 |
| 已归档项目 | 所有变更入口禁用 + 统一原因说明 |
| Skill 生产版本 | 只读；变更须新建版本或回滚 |

### 15.5 JSON 交换格式

1. 导入导出 schema 须版本化（如 `testopsCase.v1`）。
2. 破坏性变更须迁移说明或兼容层。
3. 导出文件命名遵循 5.10。

## 十六、后续沉淀建议

建议后续把本规范继续拆细为以下文档：

1. `TestOps` 名词表
2. `TestOps` 状态机规范
3. `TestOps` 组件规范
4. `TestOps` 页面模板库
5. `TestOps` 错误提示文案库
6. `TestOps` 字段问号说明库（扩展附录 G）
7. `TestOps` 删除判定与国际化文案库（扩展附录 H）

---

## 附录 A：推荐页面标题格式

统一格式建议：

1. `[模块名]`
2. `[模块名] - [操作]`

示例：

1. `项目`
2. `项目工作区`
3. `测试用例 - 新建用例`
4. `测试用例 - 用例详情`
5. `评审 - 项目评审`

## 附录 B：推荐状态文案映射

1. `draft` → `草稿`
2. `needs_update` → `待修改`
3. `approved` → `已批准`
4. `published` → `已发布`
5. `rejected` → `已驳回`

## 附录 C：推荐空状态写法

1. 暂无项目：点击`新建项目`开始，可上传资料生成测试用例，或导入 JSON 用例文件。
2. 暂无文档：上传 PRD、Swagger 或 Figma 资料后即可生成用例。
3. 暂无用例：请先上传资料生成用例，或导入已有用例文件。
4. 没有匹配结果：试试调整搜索词、状态或目录筛选条件。

## 附录 D：B 端规范借鉴映射

| B 端章节 | TestOps 章节 | 借鉴方式 |
|---|---|---|
| 一、产品设计原则 | 二、产品设计原则 | 已对齐 |
| 2.1 表格表单类页面 | 四、八 | 折叠、操作数、批量 |
| 2.2 常见数据类型 | 六 | 类型化展示规则 |
| 3.1 界面布局-PC | 四、十二 | 控制台/表格/表单 |
| 3.3 文字 | 十一、十二.4 | 字体与文案 |
| 3.4 导航区 | 十二.5 | 侧栏、分页、滚动条 |
| 3.5 功能显示区 | 五、八 | 筛选、表格、分页 |
| 3.6 按钮 | 五.6–5.10、十一 | 行为与命名 |
| 3.7–3.9 排序/顺序/查询 | 五.5、八 | 全局排序、字段序 |
| 3.11 反馈规范 | 十、十二.8 | 组件选型 |
| 3.12 导出规范 | 5.10 | 适配 JSON/列表导出 |
| 3.14–3.15 图标/快捷键 | 十二.6–12.7 | 直接借鉴 |
| 4.1 长度规范 | 6.7 | 字段上限 |
| 4.5–4.9 按钮/对齐/页面 | 八.5、十一、五.1、附录 F–I | 适配 TestOps |
| 4.2 删除规范 | 附录 H、5.7 | 判定矩阵 |
| 4.4 字段展示 | 附录 F | 各页默认列 |
| 4.7.2–4.7.3 问号/页面说明 | 附录 G、I | 首批字段与页面 |
| 4.3 层级结构 | 6.8 | 目录树 |
| 3.16 输入焦点 | 9.4 | 通用输入行为 |
| 4.9 视觉交互 | 12.9 | 按钮四态、过渡 |
| — | 十五 | TestOps 专有（追溯/评审/AI） |
| 4.11 输入字符 | 6.2 | 空格与 emoji |
| 3.10 数值精度 | — | **不借鉴**（非 ERP） |
| 3.13 打印规范 | — | **不借鉴** |
| 四、商品/菜单业务 | — | **不借鉴** |

## 附录 E：TestOps 删除确认文案模板

| 对象 | 模板 |
|---|---|
| 用例 | 确认删除用例「{title}」吗？删除后不可恢复。 |
| 文档 | 确认删除文档「{name}」吗？删除后将影响关联生成任务追溯。 |
| 项目归档 | 确认归档项目「{name}」吗？归档后默认从进行中列表隐藏。 |
| Skill | 确认删除技能「{name}」吗？ |
| 批量 | 确认删除选中的 {count} 条记录吗？ |
| 不可删除 | 说明具体关联原因，如「该用例已发布，请先退回草稿后再删除。」 |

## 附录 F：各页面默认展示列

> 借鉴 B 端 4.4。未在默认区的字段进入「列选择」面板；用户自定义后随账号保存（能力就绪后）。

| 页面 | 默认展示列（按顺序） | 列选择可追加 |
|---|---|---|
| 项目列表 | 项目名称、项目代号、描述摘要、文档数、草稿数、已发布数、状态 | 创建时间、更新时间 |
| 用例列表 | 用例标题、模块、功能点、优先级、状态、步骤数、更新时间 | 目录路径、创建时间、标签、来源文档 |
| 文档列表 | 文档名称、文档类型、来源方式、上传/链接标识、更新时间 | 关联生成任务数 |
| 生成任务列表 | 创建时间、任务状态、关联文档、模型/Skill 摘要、完成时间 | 失败原因、输入条数 |
| Skill 列表 | 技能名称、分类、当前生产版本、状态、更新时间 | 标签、绑定项目数 |
| 评审队列 | 用例标题、状态、模块、优先级、更新时间 | 评审人、最后评审时间 |
| 步骤表（用例编辑） | 序号、操作步骤、预期结果 | 测试数据、定位提示、断言 |
| 自动化计划列表 | 计划名称、状态、最近运行、下次运行 | 关联项目、创建时间 |

## 附录 G：字段问号说明（首批）

> 借鉴 B 端 4.7.2。复杂字段旁提供 `?`，hover 或点击展示说明。

| 页面 | 字段 | 说明文案 |
|---|---|---|
| 项目工作区 | 生成模式 | 基于已选文档与 Skill 配置，由 AI 自动生成测试用例草稿 |
| 项目工作区 | 导入模式 | 上传已有 JSON 用例文件，跳过 AI 生成直接进入编辑 |
| 文档资料 | 文档类型 | PRD 侧重业务场景；Swagger 侧重接口契约；Figma 侧重界面与交互 |
| 用例编辑 | 模块 / 功能点 | 用于组织用例目录与筛选；建议与 PRD 模块结构一致 |
| 用例编辑 | 优先级 | P0 为核心路径；P1 为重要分支；P2 为低频或边缘场景 |
| 用例编辑 | 定位提示 | 供 UI 自动化参考的选择器或页面位置描述，非必填 |
| 评审页 | 批准 | 内容通过评审，可进入发布；不等于已对下游生效 |
| 评审页 | 发布 | 用例对下游自动化可用；发布后核心内容默认锁定 |
| 评审页 | 退回修改 | 需补充或修正后再提交；状态变为待修改 |
| 评审页 | 驳回 | 当前版本不予通过，不进入发布 |
| Skill 详情 | 证据策略 | 仅依据明确需求/契约/界面证据生成；歧义标待确认，禁止臆造 |
| Skill 详情 | 场景分类 | 定义生成用例覆盖的场景类型，如主流程、边界、异常 |
| 生成任务 | 任务状态 | 排队中、生成中、成功、失败；失败可查看原因并重试 |
| 生成任务 | 关联文档 | 本次生成所依据的资料；与用例追溯链关联 |

## 附录 H：删除与不可删除判定矩阵

> 借鉴 B 端 4.2 方法论。允许删除用确认弹窗；不允许删除用 Toast 或 Alert，说明原因。

| 对象 | 是否允许 | 条件 | 提示方式 | 中文模板 |
|---|---|---|---|---|
| 草稿用例 | 允许 | 无下游锁定 | 确认弹窗 | 确认删除用例「{title}」吗？删除后不可恢复。 |
| 已发布用例 | 禁止或需先退回 | 下游可能引用 | Toast / Alert | 该用例已发布，请先退回草稿后再删除。 |
| 文档 | 允许（说明影响） | — | 确认弹窗 | 确认删除文档「{name}」吗？删除后将影响关联生成任务追溯。 |
| 目录节点 | 禁止 | 含子目录或用例 | Toast | 目录下存在子目录或用例，不可删除。 |
| 项目 | 归档（非物理删） | 进行中项目 | 确认弹窗 | 确认归档项目「{name}」吗？归档后默认从进行中列表隐藏。 |
| Skill（有绑定/任务） | 禁止 | 仍被项目绑定或任务引用 | Toast | 该技能仍被使用，请先解除绑定后再删除。 |
| Skill 生产版本 | 禁止直接删 | 须回滚或新建版本 | Alert | 生产版本不可删除，请发布新版本或执行回滚。 |
| Skill 回滚 | 允许 | — | Popconfirm / 确认弹窗 | 确认回滚为版本「{version}」吗？ |
| 批量记录 | 允许 | — | 确认弹窗 | 确认删除选中的 {count} 条记录吗？ |

**英文模板（预留）**：关键删除确认须预留 EN 列，与中文结构一致，如 `Are you sure you want to delete test case "{title}"? This action cannot be undone.`

## 附录 I：各页面说明文案

> 借鉴 B 端 4.7.3。置于页面标题下方，12px 辅助色，1–2 句。

| 页面 | 页面说明 |
|---|---|
| 项目列表 | 管理测试项目，进入工作区后可上传资料、生成或导入用例，并完成评审与发布。 |
| 项目工作区 | 承接当前项目的下一步动作。先上传资料或导入用例，再进入编辑与评审。 |
| 文档资料 | 上传 PRD、Swagger 或 Figma 资料。选择文档后可生成测试用例。 |
| 测试用例列表 | 浏览、筛选并进入用例编辑或评审。左侧目录树可按模块组织用例。 |
| 用例编辑 | 编辑结构化测试内容。保存后可提交评审；已发布用例的核心字段可能锁定。 |
| 评审 | 查看用例内容、填写评审意见，并执行批准、退回、驳回或发布。 |
| 生成任务 | 查看 AI 生成任务状态。失败任务可查看原因并决定是否重试。 |
| Skill 中心 | 管理测试生成技能模板与版本。项目绑定已发布的技能版本后用于生成。 |
| 设置 | 查看系统与环境配置。只读项与可编辑项以视觉区分。 |
