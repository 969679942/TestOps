# Product

## Register

product

## Users

QA 工程师、测试负责人、交付团队。在发布前需要上传需求资料、生成或导入 UI 自动化用例、评审并发布给下游脚本生成流程。

## Product Purpose

TestOps 是测试用例工作台：管理项目、资料、用例草稿、评审与发布。成功标准是用户能高效完成「建项目 → 导入/生成用例 → 编辑 → 批准 → 发布」，且界面稳定可信、不分散注意力。

## Brand Personality

专业、清晰、可靠。像 Linear / Notion 一类工具：任务优先，视觉退后。

## Anti-references

- 营销落地页的夸张动效与渐变
- 卡片套卡片的信息架构
- 紫色渐变、Inter 默认模板感
- 装饰性 hover 抬升与页面入场动画序列

## Design Principles

1. **任务优先**：每个屏幕只突出一个主操作。
2. **一致性胜过惊喜**：按钮、表单、列表、Tab 全站同一套词汇。
3. **状态完整**：hover / focus / active / disabled / loading 必须齐全。
4. **中文优先**：文案、标签、空状态面向中文用户。
5. **可交给 AI**：用例结构完整，便于下游生成 Playwright 脚本。

## Accessibility & Inclusion

WCAG AA 对比度；键盘 focus 可见；尊重 `prefers-reduced-motion`。
