# TestOps 设计整改基线文档

> 本文档用于指导 `TestOps` 当前项目的批量整改，目标是把 [TestOps产品设计规范.md](/D:/Menusifu/TestOps/TestOps产品设计规范.md) 转换为可排期、可开发、可测试、可验收的整改清单。
>
> 本文档是整改执行基线，不替代产品设计规范。发生冲突时，以 `产品设计规范` 为准；发生实现取舍时，以本基线文档的优先级和例外说明为准。

## 一、文档使用说明

### 1.1 目标

1. 为研发提供页面级和组件级整改边界。
2. 为测试提供逐条验证的检查点。
3. 为产品和设计提供整改优先级与范围控制。
4. 降低重复整改和跨页面风格分裂。

### 1.2 适用范围

当前适用于 `apps/web` 下的 Web 管理端页面与共用组件，重点覆盖：

1. 首页项目列表
2. 项目工作区
3. 测试用例列表
4. 测试用例新建 / 编辑 / 详情
5. 评审页
6. 生成任务页
7. 自动化计划页
8. 设置页
9. 共用弹窗、表格、表单、状态、空状态、导航

### 1.3 使用方式

1. 先按优先级确定整改顺序。
2. 再按页面整改清单识别受影响路由、页面组件、共用组件。
3. 每完成一个页面或一组组件，同步执行测试验收清单。
4. 如遇规范未覆盖但当前实现必须落地的例外情况，先记录到“暂不整改项 / 例外项”，再进入开发。

## 二、优先级定义

### 2.1 P0

影响主流程完成、容易误操作、会造成状态误解或明显交互断裂的问题。

判定标准：

1. 用户无法顺畅完成建项目、上传资料、生成、编辑、评审、发布主链路。
2. 关键操作缺少确认、反馈或错误定位。
3. 同类核心页面的交互行为明显不一致，影响用户判断。

### 2.2 P1

不阻断主流程，但会降低效率、增加理解成本或放大后续返工的问题。

判定标准：

1. 查询、筛选、返回定位、空状态等效率型体验不统一。
2. 同一组件在多个页面表达不一致。
3. 页面结构可用，但不符合规范基线。

### 2.3 P2

优化型、沉淀型、对后续统一有帮助，但不应阻塞 P0 / P1 的事项。

判定标准：

1. 视觉细节统一。
2. 次要信息结构调整。
3. 低频页面或暂未形成完整流程的模块优化。

## 三、整改范围映射

### 3.1 页面路由映射

| 模块 | 路由 | 页面文件 | 主要组件 |
|---|---|---|---|
| 项目列表 | `/` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/page.tsx) | [ProjectDirectory](/D:/Menusifu/TestOps/apps/web/components/project-directory.tsx) |
| 项目工作区 | `/projects/[projectId]` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/page.tsx) | [ProjectWorkspaceClient](/D:/Menusifu/TestOps/apps/web/components/project-workspace-client.tsx) |
| 文档入口 | `/projects/[projectId]/documents` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/documents/page.tsx) | 当前重定向到工作区 |
| 用例列表 | `/projects/[projectId]/test-cases` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/test-cases/page.tsx) | [TestCaseListWorkspace](/D:/Menusifu/TestOps/apps/web/components/test-case-list-workspace.tsx) |
| 新建用例 | `/projects/[projectId]/test-cases/new` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/test-cases/new/page.tsx) | [TestCaseComposer](/D:/Menusifu/TestOps/apps/web/components/test-case-composer.tsx) |
| 用例详情 | `/projects/[projectId]/test-cases/[testCaseId]` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/test-cases/[testCaseId]/page.tsx) | [TestCaseEditor](/D:/Menusifu/TestOps/apps/web/components/test-case-editor.tsx) |
| 评审页 | `/projects/[projectId]/review` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/review/page.tsx) | [ReviewEditor](/D:/Menusifu/TestOps/apps/web/components/review-editor.tsx) |
| 生成任务 | `/projects/[projectId]/generation-tasks` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/generation-tasks/page.tsx) | [GenerationTaskList](/D:/Menusifu/TestOps/apps/web/components/generation-task-list.tsx) |
| 自动化计划 | `/projects/[projectId]/automation-schedules` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/projects/[projectId]/automation-schedules/page.tsx) | 页面内列表卡片 |
| 设置 | `/settings` | [page.tsx](/D:/Menusifu/TestOps/apps/web/app/settings/page.tsx) | 设置摘要卡片、设置详情列表 |

### 3.2 共用组件映射

| 组件类 | 主要文件 |
|---|---|
| 应用骨架 / 导航 | [app-shell.tsx](/D:/Menusifu/TestOps/apps/web/components/app-shell.tsx) |
| 确认弹窗 | [confirm-action-modal.tsx](/D:/Menusifu/TestOps/apps/web/components/confirm-action-modal.tsx) |
| 项目状态操作 | [project-status-action.tsx](/D:/Menusifu/TestOps/apps/web/components/project-status-action.tsx) |
| 表格列表 | [test-case-results-table.tsx](/D:/Menusifu/TestOps/apps/web/components/test-case-results-table.tsx) |
| 评审编辑器 | [review-editor.tsx](/D:/Menusifu/TestOps/apps/web/components/review-editor.tsx) |
| 全局样式 | [globals.css](/D:/Menusifu/TestOps/apps/web/app/globals.css) |

## 四、页面整改清单

### 4.1 项目列表页

优先级：`P1`

对应规范项：

1. 项目列表页
2. 主次操作
3. 空状态规范
4. 删除 / 归档危险操作
5. 状态与反馈一致性

现状观察：

1. 当前列表已具备进行中 / 已归档切换、创建项目、Toast 反馈和归档操作。
2. 页面具备基础可用性，但页面上“项目概览”“企业控制台视图”等表达更偏展示性，项目管理主任务还可进一步收敛。
3. 归档 / 恢复反馈已有，但验收上仍需统一确认文案、状态变更后的定位与刷新策略。

目标行为：

1. 首页首屏只突出“查看项目并进入工作区”“新建项目”两个主任务。
2. 归档、恢复必须统一使用确认弹窗规范与状态反馈规范。
3. 列表空状态、归档空状态、请求失败状态的文案风格和操作出口保持统一。
4. 列表项的状态动作、进入工作区入口、统计信息结构保持稳定，不再混入装饰性表达。

测试关注点：

1. 新建项目成功后，新项目插入正确列表且 Toast 正确显示。
2. 归档后项目从进行中列表移除，并出现在已归档列表。
3. 恢复后项目从已归档列表移除，并回到进行中列表。
4. 失败时不出现“视觉成功、实际失败”。

### 4.2 项目工作区页

优先级：`P0`

对应规范项：

1. 项目工作区页
2. 页面跳转载体
3. 加载反馈
4. 空状态与下一步提示

现状观察：

1. 当前项目工作区承载上传资料生成、导入用例、流程步骤展示与下一步入口。
2. 页面已形成主工作区，但文档中心实际被折叠进工作区模式中，独立页面缺失。
3. 工作区与文档、用例、评审之间的关系在导航层面可达，但职责边界还不够清晰。

目标行为：

1. 工作区只负责承接当前项目的下一步动作，不替代所有子页面。
2. 文档、生成、评审、用例入口应明确区分“当前状态”和“下一步动作”。
3. 工作区中的主按钮和次按钮层级固定，避免同级多个强主操作抢焦点。
4. 项目已归档时，所有可变更入口统一降级为不可操作状态，并给出相同原因说明。

测试关注点：

1. `mode=generate` 和 `mode=import` 两种入口状态切换正确。
2. 已归档项目下，上传、生成、导入、新建等动作不可执行。
3. 当项目已有用例时，下一步入口与数量提示正确。

### 4.3 文档入口 / 文档页

优先级：`P0`

对应规范项：

1. 文档资料页
2. 页面跳转载体
3. 空状态规范
4. 查询和列表反馈

现状观察：

1. `/documents` 当前直接重定向到项目工作区，没有独立的文档中心承载。
2. 这会导致“文档管理”和“工作区流程承接”职责混在一起。

目标行为：

1. 明确是否保留独立文档页。
2. 若保留独立文档页：
   - 需提供文档列表、来源方式、文档类型、批量选择、生成入口。
3. 若不保留独立文档页：
   - 需在规范和导航中明确“文档中心并入工作区”，并移除误导性导航。
4. 最终不能出现“导航存在，但页面只是重定向”的半成品状态。

测试关注点：

1. 文档入口跳转结果与导航文案一致。
2. 文档列表与生成动作的关系清晰可测。
3. 空状态能明确告诉用户支持上传什么、下一步做什么。

### 4.4 测试用例列表页

优先级：`P0`

对应规范项：

1. 测试用例列表页
2. 查询操作规范
3. 表格与筛选规范
4. 查询结果反馈

现状观察：

1. 当前页面已经具备目录树、状态筛选、关键词搜索和结果计数。
2. 当前筛选项只有状态、关键词、目录，且查询标签主要通过占位和 `sr-only` 隐藏文本表达。
3. 尚未形成高级查询、查询轨迹、重置、批量操作、统一结果摘要等完整查询体系。
4. 当前结果表格字段较少，更偏“查看入口表”，而不是完整的业务列表。

目标行为：

1. 补齐显式筛选标题，避免用户只能依赖占位理解查询条件。
2. 按规范建立基础查询区和可扩展的高级查询机制。
3. 提供 `重置` 能力，并统一查询后结果反馈。
4. 若短期不做批量操作，也需在基线文档中明确“本阶段仅支持单条查看，不支持批量删除 / 批量发布”。
5. 列表字段结构统一为“识别信息 + 属性 + 状态 + 时间 + 操作”。

测试关注点：

1. 组合筛选后结果准确。
2. 清空或删除单个筛选条件后结果立即刷新。
3. 无结果时展示明确提示，而不是空白。
4. 查询后结果统计与实际条数一致。

### 4.5 新建用例页

优先级：`P0`

对应规范项：

1. 用例详情 / 编辑页
2. 表单规范
3. 提交后的错误定位
4. 已归档项目限制

现状观察：

1. 页面结构清晰，已具备面包屑、返回列表、已归档锁定。
2. 核心风险在于复杂表单的必填、错误定位、滚动定位、步骤表格错误提示是否足够统一。

目标行为：

1. 新建用例的必填、格式、重复、空格校验全部按统一规则实现。
2. 提交失败后保留用户输入，不清空。
3. 首个错误字段自动定位；若错误在折叠组或步骤表格中，需自动展开后定位。
4. 成功创建后返回列表或详情的跳转策略保持一致，并给出明确反馈。

测试关注点：

1. 必填项缺失时是否聚焦首错字段。
2. 步骤表格错误是否在单元格内提示。
3. 归档状态下是否完全阻止创建。

### 4.6 用例详情 / 编辑页

优先级：`P0`

对应规范项：

1. 用例详情 / 编辑页
2. 编辑操作规范
3. 评审面板联动

现状观察：

1. 当前详情页已把编辑器、评审面板、流程步骤放在同一上下文中。
2. 页面价值高，是编辑、评审、发布联动最集中的位置。
3. 若这里的保存、状态变更、错误反馈不统一，会放大到整个主流程。

目标行为：

1. 保存后反馈、状态刷新、侧边评审信息刷新保持一致。
2. 编辑页与评审页对同一字段的名称、校验、状态文案完全一致。
3. 已发布内容的可编辑范围必须有明确策略，不允许“视觉可编辑但提交被拒绝”的不一致。

测试关注点：

1. 保存成功后状态与内容刷新正确。
2. 已发布用例的编辑权限与页面表现一致。
3. 评审历史、当前状态、可执行动作之间无冲突。

### 4.7 评审页

优先级：`P0`

对应规范项：

1. 评审页
2. 危险操作
3. 提示与消息规范
4. 编辑操作规范

现状观察：

1. 当前评审页支持保存、批准、发布，但按钮层级还偏“并列提交”，对主次操作区分不够强。
2. 当前评审页直接在同一表单中承载多个动作，需重点梳理“保存”“批准”“发布”的反馈和前置条件。
3. 缺少对驳回 / 退回修改的完整呈现闭环。

目标行为：

1. 明确评审页的主操作顺序：保存修改、批准、发布。
2. 发布必须建立在已批准状态之上，页面和接口前置条件需一致。
3. 驳回、退回修改等负向动作应补齐，或在本阶段明确不支持。
4. 成功与失败反馈分别有稳定出口，且不覆盖用户已编辑内容。

测试关注点：

1. 未批准时不能直接发布。
2. 保存失败后原输入内容保留。
3. 状态变化后相关按钮可用性立即更新。

### 4.8 生成任务页

优先级：`P1`

对应规范项：

1. 生成任务页
2. 表单规范
3. 错误反馈

现状观察：

1. 当前页面已具备摘要卡片、任务列表、创建任务表单。
2. 任务创建入口仍依赖 `inputDocumentIds` 手工填写文档 ID，可用性偏技术向。
3. 错误信息展示有基础，但创建表单与任务结果列表的联动还不够业务化。

目标行为：

1. 文档输入方式优先改成可理解的业务选择，不再要求用户记忆或输入文档 ID。
2. 任务状态、失败原因、输入数量、模型信息的展示层级统一。
3. 创建任务成功后，有明确的队列反馈和结果刷新方式。

测试关注点：

1. 创建任务后任务列表是否即时可见。
2. 失败任务的错误信息是否可见、可理解。
3. 已归档项目下是否禁止创建任务。

### 4.9 自动化计划页

优先级：`P2`

对应规范项：

1. 自动化计划页
2. 状态展示
3. 空状态与锁定状态

现状观察：

1. 页面已具备摘要、列表、归档锁定和空状态。
2. 目前更偏只读展示页，暂未形成强编辑流程。

目标行为：

1. 保持只读页定位时，重点整改状态表达、信息分组和空状态一致性。
2. 若后续增加编辑能力，再单独补充计划配置规则。

测试关注点：

1. 无计划时空状态正确。
2. 已归档时锁定文案统一。
3. 状态和时间字段格式统一。

### 4.10 设置页

优先级：`P2`

对应规范项：

1. 设置页
2. 只读 / 可编辑区分
3. 信息分组规范

现状观察：

1. 页面已具备摘要卡片和详情分组。
2. 当前页面主要是信息查看，不是复杂设置流。

目标行为：

1. 统一摘要卡片与详情列表的信息层级。
2. 明确哪些信息是只读配置，哪些后续可扩展为可编辑配置。

测试关注点：

1. 加载失败时反馈稳定。
2. 摘要与详情信息不冲突。

## 五、组件整改清单

### 5.1 App Shell 与导航

优先级：`P1`

整改目标：

1. 全局导航和项目导航的命名、当前态、不可达态统一。
2. 不保留“导航项存在但实际重定向或无独立承载”的灰色状态。
3. 侧边栏中的项目信息、导航分组、当前页标识统一风格。

### 5.2 确认弹窗

优先级：`P0`

整改目标：

1. 删除、归档、恢复、发布等关键动作统一走确认弹窗。
2. 弹窗文案模板统一，至少包含：
   - 动作对象
   - 风险说明
   - 确认按钮文案
3. 提交中状态统一，禁止重复点击。
4. 关闭后原页面刷新策略统一。

### 5.3 表单与字段校验

优先级：`P0`

整改目标：

1. 所有核心表单统一必填标识和字段错误样式。
2. 统一首错字段定位策略。
3. 提交失败不清空输入。
4. 主要标识字段统一校验空值、首尾空格、唯一性。

### 5.4 查询栏与筛选控件

优先级：`P0`

整改目标：

1. 显式标签替代仅靠占位或 `sr-only` 的查询理解方式。
2. 统一筛选区布局、重置逻辑、结果摘要位置。
3. 可扩展为高级查询和查询轨迹。

### 5.5 表格与结果列表

优先级：`P1`

整改目标：

1. 统一表头结构、字段顺序、结果为空反馈。
2. 行内操作数控制在 2-3 个高频动作内。
3. 危险操作不直接平铺在高频主操作旁边。

### 5.6 状态徽章与状态文案

优先级：`P0`

整改目标：

1. 草稿、待修改、已批准、已发布、已驳回的颜色与文案统一。
2. 同一状态在列表、详情、评审、任务页中保持同义表达。

### 5.7 Toast、错误页、空状态

优先级：`P1`

整改目标：

1. 成功提示统一为轻量、自动消失、不阻断。
2. 页面级错误统一为错误区块或错误页。
3. 空状态统一回答“为什么为空、下一步做什么”。

### 5.8 表格对齐与 B 端表格细则

优先级：`P1`

整改目标：

1. 数值列右对齐、文本列左对齐，表头与数据一致。
2. 空单元格留空，不使用占位符。
3. 横向/纵向滚动条显性展示，固定在表格边缘。
4. 超长内容截断 + 悬浮完整展示。

### 5.9 导出与分页

优先级：`P1`

整改目标：

1. 导出命名、字段范围、格式与页面展示一致。
2. 分页提供 10/20/50/100；低数据量隐藏分页。
3. 翻页保留筛选与排序（批量勾选待后端支持）。

### 5.10 页面说明与字段问号

优先级：`P0`

整改目标：

1. 各主流程页面标题下补充 1–2 句页面说明（见规范附录 I）。
2. 复杂字段旁补充 `?` 说明（见规范附录 G 首批清单）。
3. 说明文案 12px 辅助色，hover 或点击展示，不抢占主内容。

### 5.11 删除判定与 Popconfirm

优先级：`P0`

整改目标：

1. 删除/归档/回滚行为符合附录 H 判定矩阵。
2. 不可删除场景用 Toast 或 Alert 说明原因，禁止 silent fail。
3. 行内轻量确认优先 Popconfirm，全屏危险操作用 Modal。

### 5.12 主流程步骤条与工作区优先级

优先级：`P1`

整改目标：

1. 项目工作区呈现主链路步骤条（规范 4.2.2）。
2. 待办摘要（待评审、失败任务）优先于装饰性文案（规范 4.2.1）。
3. 步骤状态与项目实际数据联动（无文档时高亮上传步骤）。

### 5.13 取消 / 返回 / 跨页记忆

优先级：`P1`

整改目标：

1. 取消、返回、× 语义分离（规范 5.11）。
2. 有未保存修改时取消须二次确认。
3. 列表→详情→返回须保留筛选、分页、目录选中（规范 5.12）。

## 六、测试验收清单

### 6.1 查询验收

1. 每个可筛选页面的筛选项都有明确标题。
2. 查询后结果回到第一页。
3. 查询后保留当前筛选条件。
4. `重置` 可清空全部筛选并恢复默认状态。
5. 无结果时显示明确提示，不显示空白区域。

### 6.2 删除验收

1. 删除前必须弹出确认。
2. 单条删除文案带对象名称，批量删除文案带数量。
3. 删除成功后列表立即刷新，记录消失。
4. 删除失败时不出现假成功状态。
5. 有关联不可删除时，用户能看到明确原因。

### 6.3 表单验收

1. 必填项缺失时提交失败并定位首错字段。
2. 字段错误文案贴近字段显示。
3. 提交失败后用户输入仍保留。
4. 步骤表格内的错误能在对应单元格看到。

### 6.4 状态流转验收

1. 未批准前不可发布。
2. 已归档项目下不可执行变更型动作。
3. 状态变化后按钮可用性和状态展示同步更新。

### 6.5 页面反馈验收

1. 成功操作有 Toast 或页面级轻反馈。
2. 页面级加载失败有明确错误提示。
3. 空状态提供明确下一步入口。

### 6.6 B 端借鉴专项验收

1. 筛选条件标题右对齐或显式标签，每行 2–4 个条件。
2. 表格数值列右对齐、文本列左对齐，表头与数据对齐一致。
3. 表格空单元格不使用 `-` / `/` 占位。
4. 分页默认 10/20/50/100；数据 ≤10 条时可隐藏分页。
5. 导出文件命名符合 `{页面标题}-{日期}{流水号}` 规则。
6. 按钮命名符合 `+ 新增` / `保存` / `确定` / `选择` 统一口径。
7. 删除确认文案使用附录 E 模板，带对象名称或数量。
8. 主流程按钮不以纯图标单独承载。

### 6.7 页面认知与删除矩阵验收

1. 主流程页面均有页面说明（附录 I）。
2. 附录 G 首批字段均有问号或等效说明。
3. 删除/归档行为符合附录 H；不可删除有明确原因。
4. 项目工作区有主流程步骤条或等效进度表达。
5. 列表→详情→返回保留筛选与分页上下文。

## 七、建议整改顺序

### 第一阶段

1. 确认弹窗统一
2. 表单校验与错误定位统一
3. 测试用例列表查询区整改
4. 评审页主次操作与状态约束整改

### 第二阶段

1. 项目工作区信息层级整改
2. 用例详情 / 编辑页状态与保存反馈整改
3. 生成任务页表单业务化整改

### 第三阶段

1. 项目列表页表达收敛
2. 自动化计划页与设置页统一化整改
3. 文档中心是否独立承载的结构性决策

## 八、暂不整改项 / 例外项

1. 自动化计划页当前按只读信息页处理，不在本轮强行扩展为完整配置系统。
2. 设置页当前以信息展示为主，不在本轮引入复杂编辑交互。
3. 若某些删除 / 发布限制依赖后端能力，本轮先统一前端提示与禁用态，不强行伪造业务规则。
4. 若文档中心最终决定并入项目工作区，需要同步更新导航与规范，不保留双入口。

## 九、交付要求

每项整改完成后，至少同步交付以下内容：

1. 代码变更
2. 对应页面或组件的验收说明
3. 若涉及主流程变更，补充自动化或手工测试点
4. 若某条规范因现状受限无法落地，需在本文件追加例外说明

## 十、第一阶段验收记录

验收时间：2026-07-02

代码位置：`D:\Menusifu\TestOps\.worktrees\codex-p0-main-chain`

分支：`codex/p0-main-chain`

### 10.1 已完成项

1. 表单校验与错误定位统一：新建用例表单提交失败后可定位首个错误字段，避免用户只看到笼统失败提示。
2. 测试用例列表查询区整改：筛选项已补显式标签，结果区已补结果摘要，已提供 `重置` 能力。
3. 评审页动作补齐：评审页已展示 `退回修改`、`驳回`、`批准`、`发布`，并补充 `评审意见` 输入。
4. 评审页负向动作闭环：`退回修改` 对应 `request_change`，`驳回` 对应 `reject`，提交时读取评审意见，未填写时使用默认评审文案。
5. 评审队列默认选择：进入评审页时默认选择第一条未发布用例，减少空页面误解。
6. 详情页评审面板确认统一：用例详情侧边评审面板的 `驳回`、`发布用例` 已从原生 `window.confirm` 改为共用 `ConfirmActionModal`。
7. Skill 生产发布确认统一：`发布为生产版本` 已改为先打开共用确认弹窗，再提交原有发布 action。
8. 文档删除确认统一：文档资料列表的单条删除已改为先打开共用确认弹窗，文案包含文档名称和追溯影响。
9. Skill 回滚确认统一：`回滚为当前生产版本` 已改为先打开共用确认弹窗，再提交原有回滚 action。
10. 项目归档 / 恢复文案复核：确认弹窗已补齐影响说明，确认按钮统一为 `确认归档` / `确认恢复`。
11. 评审页编辑器确认统一：项目级评审页 `驳回`、`发布` 已改为先打开共用确认弹窗，再提交原有评审 / 发布 action。
12. 评审页发布前置条件：项目级评审页与用例详情评审面板均已补齐 `未批准不可发布` 前端约束；未批准状态不展示发布按钮，并展示 `请先批准用例，再执行发布。` 辅助提示。

### 10.2 部分完成 / 待继续项

1. 确认弹窗统一：当前已有共用确认弹窗与项目归档 / 恢复 / 删除、详情页评审面板发布 / 驳回、项目级评审页发布 / 驳回、Skill 生产发布 / 回滚、文档删除动作覆盖；仍需继续核查后续新增删除类或回滚类动作。
2. 评审页发布接口前置条件：前端已完成 `未批准不可发布` 约束；接口层前置条件仍需结合后端能力继续核查。
3. 状态文案统一：评审页已补负向动作文案，但列表、详情、评审、任务页的状态徽章颜色和文案仍需做全站一致性核查。
4. 批量删除 / 批量发布：本阶段仍按单条操作处理，不引入批量能力。

### 10.3 验证结果

1. 前端全量测试已通过：`npm test`
2. 结果：31 个测试文件通过，113 条测试通过。
3. 前端 lint 已通过：`npm run lint`
4. 聚焦回归已通过：表单首错定位、用例列表查询重置、评审页动作、项目级评审页确认弹窗、项目级评审页未批准不可发布、详情页确认弹窗、详情页未批准不可发布、Skill 发布 / 回滚确认、文档删除确认、项目归档 / 恢复确认文案、页面渲染相关测试均通过。

## 十一、第一阶段交付摘要

### 11.1 本阶段交付内容

1. 新增交互测试环境依赖 `jsdom`，支撑真实 DOM 交互类测试。
2. 新增新建用例表单交互测试，覆盖空表单提交后的首错聚焦。
3. 新增用例列表交互测试，覆盖显式筛选标签、结果摘要和重置按钮。
4. 扩展评审编辑器测试，覆盖 `退回修改`、`驳回` 和评审意见字段。
5. 扩展页面测试，确认评审工作台默认草稿选择、评审动作展示和评审意见字段可见。
6. 新增详情页评审面板交互测试，确认 `驳回`、`发布用例` 使用统一确认弹窗，不再调用原生浏览器确认框。
7. 新增 Skill 发布交互测试，确认 `发布为生产版本` 使用统一确认弹窗。
8. 新增文档删除交互测试，确认单条文档删除前必须先展示统一确认弹窗。
9. 新增 Skill 回滚交互测试，确认 `回滚为当前生产版本` 使用统一确认弹窗。
10. 新增项目归档 / 恢复交互测试，确认弹窗展示完整影响说明和明确确认按钮。
11. 新增评审编辑器交互测试，确认 `驳回`、`发布` 使用统一确认弹窗，确认后保留当前表单数据提交原 action。
12. 扩展评审状态流转交互测试，确认项目级评审页和用例详情评审面板在未批准状态下均不可直接发布。

### 11.2 验收口径

1. 研发验收：以 worktree 中代码和测试结果为准。
2. 测试验收：优先验证主链路页面，不先扩大到自动化计划、设置页等 P2 页面。
3. 产品验收：重点确认文案、按钮层级、负向动作命名是否符合 `TestOps产品设计规范.md`。

### 11.3 下一批整改建议

下一批进入 `B 端规范借鉴第二轮（P0 认知层）`，优先覆盖：

1. 附录 I 页面说明（项目工作区、用例列表、评审、Skill 中心）。
2. 附录 G 首批字段问号（评审动作、Skill 证据策略、生成任务状态）。
3. 附录 H 删除矩阵（已发布用例不可删、目录不可删、Skill 绑定不可删）。
4. 附录 F 用例列表 / 项目列表默认列与列选择面板。
5. 取消 / 返回语义与跨页记忆（B21、B23）。

## 十二、B 端规范借鉴整改项

> 来源：[2026-07-05-b-end-design-adaptation-design.md](/D:/Menusifu/TestOps/docs/superpowers/specs/2026-07-05-b-end-design-adaptation-design.md)  
> 规范依据：更新后的 [TestOps产品设计规范.md](/D:/Menusifu/TestOps/TestOps产品设计规范.md) 附录 D–I、十五及五.11–5.13、4.2.1–4.2.2 章节。

### 12.1 P0（与主流程体验强相关）

| 序号 | 整改项 | 影响范围 | 验收要点 |
|---|---|---|---|
| B1 | 筛选区显式标签 + 布局 | 用例列表、项目列表 | 每行 2–4 条件；标题可读；高级查询轨迹 |
| B2 | 表格字段对齐 | 所有 data-table | 数值右对齐；表头与列一致 |
| B3 | 删除/归档文案模板 | 确认弹窗全站 | 符合附录 E；带对象名 |
| B4 | 空单元格展示 | 表格组件 | 不用 `-`/`/` 占位 |
| B5 | 查询结果摘要位置 | 列表页 | 总条数/状态统计在表头下或摘要区 |

### 12.2 P1（效率与一致性）

| 序号 | 整改项 | 影响范围 | 验收要点 |
|---|---|---|---|
| B6 | 按钮命名统一 | 全站按钮 | 见规范 11.3 |
| B7 | 分页控件 | 列表页 | 10/20/50/100；≤10 条可隐藏 |
| B8 | 导出命名 | 用例/报表导出 | `{标题}-{日期}{流水号}` |
| B9 | 字体层级 CSS | globals.css、页面标题 | 见规范 12.4 |
| B10 | 行内操作收纳 | 表格操作列 | 2–3 个高频 + 更多 |
| B11 | 超长文本截断 + Tooltip | 表格、列表 | 悬浮完整内容 |

### 12.3 P2（增强型）

| 序号 | 整改项 | 影响范围 | 验收要点 |
|---|---|---|---|
| B12 | 列显隐记忆 | 用例列表 | 随账号保存（依赖后端） |
| B13 | 分页/筛选记忆 | 列表页 | 跨页签返回保留（依赖后端） |
| B14 | 快捷键 Esc/Enter | 弹窗、表单 | 见规范 12.7 |
| B15 | 全局滚动条样式 | globals.css | 8px 宽、圆角、中性色 |
| B16 | 步骤条组件 | 多步向导（若有） | 仅线性流程 |

### 12.4 第二轮补充（认知与页面级）

| 序号 | 优先级 | 整改项 | 规范依据 |
|---|---|---|---|
| B17 | P0 | 各页默认展示列 | 附录 F |
| B18 | P0 | 页面说明文案 | 附录 I |
| B19 | P0 | 字段问号（首批） | 附录 G |
| B20 | P0 | 删除判定矩阵落地 | 附录 H |
| B21 | P0 | 取消/返回/× 语义 | 5.11 |
| B22 | P1 | 筛选控件细则 | 5.5（日期范围、精确匹配、已选 X 项） |
| B23 | P1 | 跨页上下文记忆 | 5.12 |
| B24 | P1 | 导入进度与部分成功反馈 | 5.13 |
| B25 | P1 | 工作区步骤条 + 待办优先级 | 4.2.1–4.2.2 |
| B26 | P1 | 目录层级删除校验 | 6.8 |
| B27 | P1 | Popconfirm / Badge / Alert | 10.6 |
| B28 | P1 | 导航待办 Badge | 待评审数、失败任务数 |
| B29 | P2 | 输入焦点行为 | 9.4 |
| B30 | P2 | 按钮四态与页面过渡 | 12.9 |

### 12.5 明确不纳入本轮

1. 商品/菜单/价格/税率/打印相关 B 端规则。
2. ERP 单据明细 Enter 增行、Tab 行切换等录入快捷键。
3. 多页签工作台（TestOps 保持侧栏 + 面包屑）。
